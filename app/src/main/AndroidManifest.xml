<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="escalante.roberto.behealthy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Sign_in" />
        <activity android:name=".Alerta" />
        <activity android:name=".Ejercicio" />
        <activity android:name=".Menu" />
        <activity android:name=".Medicamentos" />
        <activity android:name=".AgregarMedicamente" />
        <activity android:name=".Dieta" />
        <activity android:name=".pantallaGlucosa" />
        <activity android:name=".plan2" />
        <activity android:name=".plan" />
        <activity android:name=".splash_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>